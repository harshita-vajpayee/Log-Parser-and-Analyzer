Purpose: Read CrowdStrike JSON-line style logs, filter by event types, extract fields like timestamp, hostname, alert IDs, and export results to JSON or CSV.

Features

Stream-based parsing for large files

Simple filtering by event type

Outputs JSON or CSV

CLI usage